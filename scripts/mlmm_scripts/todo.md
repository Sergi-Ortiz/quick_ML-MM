- mlmm_setup.py
    - define ML-MM region (one command per each frame!!!)
    - obtain frozen_atoms list (for each structure)
    - copy opt.py, (gsm.py to perform GSM with pyGSM, not ready), all .yaml
    - Obtain product structure (should be automatized!!)
    - commands.txt (with all the commands needed for the workflow, as in tutorial.ipynb)

- Add tested .yaml workflow for ALOX15. 

- add mlmm_example
    - tutorial.md (similar to chlorinemutase.md)
    - venv_setup.md (mlmm, UMA, pyGSM, EMDA.)

